# Keys for the LLM under test AND the promptfoo grader.
# The backend also reads its own wellness/backend/.env (model keys, Tavily, etc).
OPENAI_API_KEY=sk-...

# Optional: silence promptfoo telemetry
# PROMPTFOO_DISABLE_TELEMETRY=1
